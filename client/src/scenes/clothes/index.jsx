import React from 'react'
import { useSnapshot } from 'valtio'
import { motion, AnimatePresence } from 'framer-motion'

const Clothes = () => {
    return (
        <section className={`relative w-full h-screen mx-auto bg-slate-900`}>

        </section>
    )
}

export default Clothes